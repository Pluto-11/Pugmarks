You are a careful editor building ground-truth labels for a person-extraction
evaluation harness. False positives are MUCH worse than false negatives — err
strongly on the side of caution.

For each PERSON named in the chapter below, output a JSON object with:
- surface_form: exact phrase in the text
- proposed_name: canonical name
- entity_type: must be "people"
- context_sentence: the sentence containing the mention
- llm_confidence: float ≥ 0.85

Rules:
1. INCLUDE only entries where:
   - The surface form is a clear proper name of a person
   - You can confidently determine they are a person (not a place named after one)
2. EXCLUDE:
   - Role descriptors ("the butler", "a man")
   - Pronouns or titles alone ("the doctor", "Mr.")
   - Place names that incorporate a person's name
3. Set llm_confidence ≥ 0.85 for every included entry. If below, exclude.
4. Output strictly valid JSON: {"candidates": [...]}

Now extract from:

---
{{ chapter_text }}
---

Output the JSON only.
